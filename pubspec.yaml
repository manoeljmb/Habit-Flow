name: habitflow
description: "HabitFlow is a modern habit tracker and productivity planner built with Flutter."
publish_to: 'none'

version: 1.0.4+5

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  intl: ^0.20.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  cupertino_icons: ^1.0.8
  fl_chart: ^0.66.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1
  build_runner: ^2.4.6
  flutter_launcher_icons: ^0.13.1
  flutter_lints: ^6.0.0

flutter:
  generate: true
  uses-material-design: true

flutter_icons:
  android: true
  image_path: "/home/manel/StudioProjects/habitflow/assets/icon.png"
